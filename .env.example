# Frontend
# (Optional) Cesium Ion token (not required for self-hosted terrain)
VITE_CESIUM_ION_TOKEN=

# (Optional) Local terrain tile server URL (self-hosted quantized-mesh tiles)
VITE_TERRAIN_URL=http://localhost:8082

# Backend (phase 2+)
BACKEND_URL=http://localhost:3001
